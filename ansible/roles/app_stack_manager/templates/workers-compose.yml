name: worker_app_bot

services:
  bot:
    image: ghcr.io/{{ github_repository_name }}-bot:{{ container_image_version }} 
    container_name: worker_app_bot
    volumes: 
      - ~/cookies.txt:/code/cookies.txt:ro
    command: python SiteSender.py
    restart: always
    ports:
      - "5000:5000"
    tmpfs:
      - /tmp
    environment:
      - DB_NAME=bot_db
      - DB_USER=admin
      - DB_PASSWORD={{ vault_db_password }}
      - DB_HOST={{ groups['masters'] | map('extract', hostvars, 'ansible_host') | first }}
      - DB_PORT=5432
      - BOT_TOKEN={{ vault_vk_token }}
      - MY_NODE_NAME={{ inventory_hostname }}
      - YTDLP_JS_RUNTIME=node
