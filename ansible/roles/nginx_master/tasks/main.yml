---
- name: Generating or copying nginx.conf
  ansible.builtin.template:
    src: nginx.conf.j2
    dest: "{{ app_dir }}/nginx.conf"
  notify: Reload nginx config # вызов хендлера с назв. "Reload nginx config". Он будет вызван если эта таска вернет "changed", то есть, когда файл изменился.
